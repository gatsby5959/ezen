package Day11;

import java.util.Scanner;

import Day10.Method07_lotto;

public class BaseBallGame {
	public static void main(String[] args) {
		/*
		 * 야구게임 메서드화
		 * 사용자번호는 직접 입력
		 * 컴퓨터번호는 랜덤생성 (1~9, 중복X)
		 */

		Scanner scan = new Scanner(System.in);
		int comNum[] = new int[3]; //컴퓨터 숫자 배열
		int myNum[] = new int[3]; //내 배열
		int max =9, min =1;
		int s = 0, b = 0;
		
		

	}
	
	
	/*
	 * 랜덤 배열 생성 메서드
	 * 범위를 주지 않고 -- min, max를 이용하여 범위를 설정
	 * min = 1, max = 9 
	 */
	public static int random(int min, int max){
		if(max < min) { //min max가 바뀌었다면...
			int tmp = max;
			max = min;
			min = tmp;
		}
		return (int)(Math.random()*(max-min+1))+min; // 개수)+시작
	}
	
	/*
	 * 랜덤 배열 중복되지 않게 생성
	 * isContain을 Method07_lotto 가져와서 사용
	 * 매개변수 : 배열, int min , int max
	 * 리턴 X = void
	 */
	public static int random(int[] intarr, int min , int max){
		// 객체 생성 후 메서드 호출 (일반적인 방법)
		Method07_lotto me = new Method07_lotto();
		me.randomArray(intarr);
		me.printArray(comNum);
		
//		System.out.println("--myNum--");
//		Method07_lotto.randomArray(myNum);
//		Method07_lotto.printArray(myNum);
	}

	
	
	/*
	 * 스트라이크 개수를 카운트하는 메서드
	 * comNum, myNum 주고, 번호와 위치가 일치하면 count하는 메서드
	 * 리턴타입 => int
	 */
	
	/*
	 * 볼 개수 카운트하는 메서드
	 * comNum, myNum 주고, 번호만 일치하면 count하는 메서드
	 */
	
	
	/*
	 * 배열 출력 메서드 
	 */
	
	



	
	
	
	
	
	
	
}
